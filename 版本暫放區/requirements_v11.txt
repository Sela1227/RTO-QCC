# 放射線治療體重追蹤系統 - 依賴套件
# 版本：2.0.0 (跨平台版)
# 支援：Windows / macOS / Linux

# 核心框架（必要）
flet>=0.21.0

# Excel 匯出（必要）
openpyxl>=3.1.0

# PDF 生成（必要）
reportlab>=4.0.0

# 日期處理（必要）
python-dateutil>=2.8.0

# 圖片處理（必要）
Pillow>=10.0.0

# 圖表（可選，沒有也能運行）
matplotlib>=3.7.0

# ================================================
# 安裝指令：
#
# 完整安裝（推薦）：
#   pip install -r requirements.txt
#
# 最小安裝（如果 matplotlib 有問題）：
#   pip install flet openpyxl reportlab python-dateutil Pillow
#
# macOS 額外步驟（如果遇到問題）：
#   pip install --upgrade pip
#   pip install flet --force-reinstall
#
# 執行程式：
#   python main.py
# ================================================
